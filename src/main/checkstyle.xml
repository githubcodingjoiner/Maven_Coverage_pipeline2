<?xml version="1.0"?>
<!DOCTYPE module PUBLIC "-//Puppy Crawl//DTD Check Configuration 1.3//EN" "http://checkstyle.sourceforge.net/dtds/configuration_1_3.dtd">
<configuration>
    <module name="Checker">
        <module name="TreeWalker">
            <!-- Enforce method naming convention -->
            <module name="MethodName">
                <property name="format" value="^[a-z][a-zA-Z0-9]*$"/>
            </module>
            <!-- Check for file tab characters -->
            <module name="FileTabCharacter"/>
            <!-- Enforce line length limit (100 characters) -->
            <module name="LineLength">
                <property name="max" value="100"/>
                <property name="ignorePattern" value="^package|^import"/>
            </module>
            <!-- Enforce usage of spaces for indentation -->
            <module name="Indentation">
                <property name="basicOffset" value="4"/>
                <property name="lineWrappingIndentation" value="4"/>
            </module>
            <!-- Enforce that class names start with an uppercase letter -->
            <module name="ClassName"/>
            <!-- Ensure that each class has a javadoc comment -->
            <module name="JavadocType"/>
        </module>
    </module>
</configuration>
